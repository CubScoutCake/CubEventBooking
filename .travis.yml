
language: php

sudo: required

php:
  - 7.0

env: DB=mysql

before_script:
  - sh -c "composer require 'cakephp/cakephp-codesniffer:dev-master'"
  - phpenv rehash
  - composer install
  - composer update

script:
  - sh -c "vendor/bin/phpcs -p --extensions=php --standard=vendor/cakephp/cakephp-codesniffer/CakePHP ./tests"

notifications:
  email: true

addons:
    code_climate:
        repo_token: 64eab16091b089fe68b6a05dfb6155fe78f5e00ffb556b55535f5eb95ba8f7a3